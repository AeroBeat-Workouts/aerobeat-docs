# Repository Topology

AeroBeat uses a lane-based polyrepo structure.

## Scope note

The topology can accommodate more features and platforms than the current product slice actively ships. Today, the active gameplay focus is camera-first Boxing + Flow, while other feature/input/platform repos may remain as future-support lanes.
