# Repo Structure Reference `/src/` Fix

**Date:** 2026-04-20  
**Status:** In Progress  
**Agent:** Chip 🐱‍💻

---

## Goal

Fix the concrete implementation repo example in `docs/architecture/repo-structure-reference.md` so it reflects the actual `/src/` convention used by repos like `aerobeat-input-mediapipe-python` instead of stale `/scripts/` wording.

---

## Tasks

### Task 1: Patch the concrete repo example to use `/src/`

**Bead ID:** `Direct`  
**SubAgent:** `primary`  
**Prompt:** Update `docs/architecture/repo-structure-reference.md` so the concrete implementation repo examples use the real `/src/` layout rather than stale `/scripts/` references, and keep the wording aligned with the current repo conventions.

**Status:** ⏳ Pending

**Results:** Not started.

---

## Final Results

**Status:** ⏳ Pending

**What We Built:** Pending.

---

*Completed on Pending*
